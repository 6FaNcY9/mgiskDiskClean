# devenv.yaml
# Pin inputs for reproducibility
inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixpkgs-unstable
  devenv:
    url: github:cachix/devenv

# Disable devenv's cachix substituter injection —
# caches are managed centrally in nix-modules/core/nix.nix
cachix:
  enable: false
