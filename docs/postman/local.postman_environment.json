{
  "name": "MrijaClient — local",
  "values": [
    {
      "key": "base_url",
      "value": "http://127.0.0.1:8080",
      "enabled": true,
      "description": "Default local client address (--bind 127.0.0.1 --port 8080)"
    },
    {
      "key": "api_key",
      "value": "",
      "enabled": true,
      "description": "Paste the key printed by python -m mrija_client at startup (set MRIJA_API_KEY to reuse)"
    },
    {
      "key": "db_path",
      "value": "",
      "enabled": true,
      "description": "Absolute path to a .sqlite file for the /api/open request"
    }
  ],
  "_postman_variable_scope": "environment"
}
