# generated files — never commit these
reports/
logs/
nginx/.htpasswd
nginx/nginx.pid

# devenv
.devenv/
.devenv.flake.nix
devenv.lock

# web app — local secrets (never commit)
web/config/local.php

# local secrets — SSH passwords, env overrides
.env
.env.push

# local mailbox data (large; outside webroot)
data/
!data/mailboxes.txt.example

# git worktrees
.worktrees/

# IDE / tooling
.idea/
.superpowers/
.sisyphus/
